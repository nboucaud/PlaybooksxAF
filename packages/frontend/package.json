{
  "name": "frontend-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "admin",
    "apps/mobile"
  ],
  "scripts": {
    "build:web": "cd apps/web && yarn install && yarn build",
    "build:admin": "cd admin && yarn install && yarn build",
    "build:mobile": "cd apps/mobile && yarn install && yarn build",
    "build:all": "yarn build:web && yarn build:admin && yarn build:mobile"
  }
}
